{
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "/opt/venv/bin/uvicorn server.app:app --host 0.0.0.0 --port ${PORT:-8080} --log-level info",
        "restartPolicyType": "ON_FAILURE"
    }
}
