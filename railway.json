{
    "build": {
        "builder": "NIXPACKS",
        "nixpacksPlan": {
            "startPhase": "python server/app.py"
        }
    },
    "deploy": {
        "startCommand": "python server/app.py",
        "restartPolicyType": "ON_FAILURE"
    }
}
